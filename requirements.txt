# ── requirements.txt — Dependencies for the team_agent multi-agent system ────

# LangChain / LangGraph core
langgraph>=0.2.0
langchain>=0.3.0
langchain-core>=0.3.0
langchain-community>=0.3.0
langchain-groq>=1.1.0
langchain-openai>=0.2.0          # Cerebras / OpenRouter (API compatible OpenAI)

# Web backend
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
websockets>=12.0

# Memory backends
redis>=5.0.0                     # L2 working memory
chromadb>=0.5.0                  # L3 long-term memory
psycopg2-binary>=2.9.0           # L4 identity store (Postgres)

# Embeddings
sentence-transformers>=5.0.0
huggingface-hub>=1.5.0

# MCP / Serena integration
langchain-mcp-adapters>=0.1.0

# Tracer (local package — no PyPI dependency)
-e ../04_Tracer

# Utilities
python-dotenv>=1.0.0
pydantic>=2.12.0
typing_extensions>=4.14.0

